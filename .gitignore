# OS files
.DS_Store
Thumbs.db
*.swp
*~

# Build directories
/bin/
/build/
/out/
# React build output (embedded into Go, but not tracked in Git)
web/dist/
web/build/
wordlists/big.txt
wordlists/actions.txt
wordlists/directory-list-2.3-small.txt
wordlists/common.txt

# Go build artifacts
*.test
*.out
*.exe
*.dll
*.so
*.dylib

# Dependency directories
vendor/
node_modules/

# IDEs and editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sublime-project
*.sublime-workspace

# Logs and temp files
*.log
logs/
tmp/
temp/

# Environment / secrets (CRITICAL)
.env
# Keep the example but ignore the real one
!.env.example
*.key
*.pem
*.crt
*.p12
credentials.json
secrets.*
# Local CA store generated by the suite
.dirfuzz-suite/ca/

# Local Data Store & Scan Results
# Ignore the local state folder if created in project root
.dirfuzz-suite/
# Local MCP audit logs may contain sensitive request metadata.
dirfuzz-audit.jsonl
**/dirfuzz-audit.jsonl
# Ignore various result formats
*.jsonl
*.csv
*.url
/scans/
scans/
state.jsonl
results.jsonl

# Coverage and profiling
coverage.out
*.coverprofile
*.prof

# Archives
*.zip
*.tar.gz
*.tar
*.tgz
*.7z

# Databases
*.sqlite3
*.db

# Local tooling
.codex


/wordlists/common.txt
wordlists/common.txt
